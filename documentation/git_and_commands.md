# GIT

### How to merge. From transfer code from development to main
```git
git branch              #Check you are in development branch
git checkout main       #Go to main branch
git pull origin main    #Get latest version from main
git merge development   #Merge development with main, fix confilicts if there is any
git push origin main    #Push changes to main 
```